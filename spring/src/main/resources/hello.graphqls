
type User {
    id: Int
    name: String
    age: Int
    city: String
}

type Query {
    allUsers: [User]
    user(id: Int): User
}

type Mutation {
    addUser(name: String, age: Int, city: String): User
    removeUser(id: Int): User
}

